<%# app/views/patients/edit.html.erb%>



<%# this file is the frontend to allow 
manipulation of patient details following update policy 
of CRUD%>



<%# uses bootstap for styling in classes tag included in
application.html.erb in layouts folder, gem also %>


<%# backend JS link from assets/javascripts%>
<%= javascript_include_tag "edit_patient_form", defer: true %>


<div class="container d-flex justify-content-center mt-5">
  <div class="card shadow rounded-4 p-4" style="max-width: 600px; width: 100%;">
    
    
    <h2 class="text-primary fw-bold text-center mb-4">
      <%= @patient.new_record? ? "New Patient" : "Edit Patient" %>
    </h2>

    <%= form_with model: @patient, local: true, html: { multipart: true } do |form| %>
      <% if @patient.errors.any? %>
        <div class="alert alert-danger">
          <h5><%= pluralize(@patient.errors.count, "error") %> prevented this patient from being saved:</h5>
          <ul>
            <% @patient.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>




      <!-- Living Status -->
      <div class="mb-3">
        <%= form.label :living_status, "Living Status" %>
        <%= form.select :living_status, ["Normal", "Went Away Treated", "Died"], {}, class: "form-select" %>
      </div>




      <!-- Name -->
      <div class="mb-3">
        <%= form.label :name %>
        <%= form.text_field :name, class: "form-control" %>
      </div>




      <!-- Age -->
      <div class="mb-3">
        <%= form.label :age %>
        <%= form.number_field :age, class: "form-control" %>
      </div>




      <!-- Gender -->
      <div class="mb-3">
        <%= form.label :gender %>
        <%= form.select :gender, ["Male", "Female", "Other"], {}, class: "form-select" %>
      </div>




      <!-- Vaccinated -->
      <!-- boolean true or false in DB -->
      <div class="mb-3">
        <div class="form-check">
          <%= form.check_box :vaccinated, { class: "form-check-input" } %>
          <%= form.label :vaccinated, "Vaccinated", class: "form-check-label" %>
        </div>
      </div>




      <!-- Diagnosis -->
      <div class="mb-3">
        <%= form.label :diagnosis %>
        <%= form.text_area :diagnosis, class: "form-control" %>
      </div>




      <!-- Critical Status -->
      <!-- includes warning signs to showcase who is in more need
      --> 
      <div class="mb-3">
        <%= form.label :critical_status %>
        <%= form.select :critical_status, ["Mild", "Critical", "Highly Critical", "Immediate"], {}, class: "form-select" %>
      </div>





      <!-- Treatment Status -->
      <!-- if status is changed the background
      and title appears to showcase if they are dead or cured -->
      <div class="mb-3">
        <%= form.label :treatment_status %>
        <%= form.select :treatment_status, ["Cured", "Not Treated", "Dead"], {}, class: "form-select", id: "treatment_status_select" %>
      </div>




      <!-- Cure Fields -->
      <!-- explain reason of curement and date -->
      <div id="cure_fields" style="display: none;">
        <div class="mb-3">
          <%= form.label :treatment_date, "Date of Cure" %>
          <%= form.date_field :treatment_date, class: "form-control", value: @patient.treatment_date&.strftime("%Y-%m-%d") %>
        </div>
        <div class="mb-3">
          <%= form.label :treatment_reason, "Reason for Cure" %>
          <%= form.text_area :treatment_reason, class: "form-control" %>
        </div>
      </div>





      <!-- Death Fields -->
      <!-- explains when user died/date and reason
      of death -->
      <div id="death_fields" style="display: none;">
        <div class="mb-3">
          <%= form.label :date_of_death, "Date of Death" %>
          <%= form.date_field :date_of_death, class: "form-control", value: @patient.date_of_death&.strftime("%Y-%m-%d") %>
        </div>
        <div class="mb-3">
          <%= form.label :reason_of_death, "Reason of Death" %>
          <%= form.text_area :reason_of_death, class: "form-control" %>
        </div>
      </div>






      <!-- Profile Picture -->
      <!-- profile pic is
      saved locally via activeStorage and 
      on AWS bucket/user in deployment -->
      <div class="mb-3">
        <%= form.label :profile_pic %>
        <%= form.file_field :profile_pic, class: "form-control" %>
      </div>







      <!-- Button to return -->
      <div class="d-flex justify-content-between mt-4">
        <%= link_to "Back to Patients", patients_path, class: "btn btn-outline-secondary" %>
        <%= form.submit class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
